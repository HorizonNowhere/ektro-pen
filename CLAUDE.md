# EKTRO

> **Type like you.**
>
> 一个会长成你数字分身的拼音输入法。

---

## ⚠ 写在最前 — 三戒（不可妥协）

> 任何贡献者、任何 AI 助手、任何未来的我自己，在动手改这个项目之前，都必须先读懂这三条。
> 任何与这三条冲突的功能、PR、依赖，都直接拒绝。

```
   ┌────────────────────────────────────────────────────┐
   │                                                    │
   │   ① 不打断视线 (Calm typing)                       │
   │      默认无候选窗。打字这件事，永远安静。          │
   │      所有打字时的 UI 服从这一条。                  │
   │                                                    │
   │   ② 记忆属于你 (Memory is yours)                   │
   │      你打的每个字都被本机完整记下来 ——              │
   │      离线、明文、可视、可导出、可删除。            │
   │      选择登入 ektro 时, 你可以带着它上来           │
   │      喂养你的数字分身。不登入也行 ——               │
   │      这份记忆始终是你的。                          │
   │                                                    │
   │   ③ 越用越是你 (It becomes you)                    │
   │      EKTRO 不掩饰自己是个 AI 产品。               │
   │      它长成一个真正懂你、能替你说话的 AI Agent。   │
   │                                                    │
   └────────────────────────────────────────────────────┘
```

---

## 一、产品定义

**EKTRO** 是一个拼音输入法，也是你 AI 数字分身的入口。它的**反叛性主张**有两层：

> 其一：输入法不该让你选字。候选窗是机器不够聪明的赔礼道歉。
> 30 年来没人把"每打 4 个字就要看一次候选窗"当成 bug —— 我们把它当 bug。
>
> 其二：你每天打几千个字，最贴近"你"的数据却被丢弃。EKTRO 把它接住，
> 同步到 ektroai.com 上你的数字分身 —— 用得越久，分身越像你，
> 直到它能替你说出你会说的话。输入是入口，分身是终点。

### 核心交互

```
   普通打字：
   ─────────
     输入 → inline 直接显示中文，无候选窗，无中断

   纠错：
   ─────
     错了 → 按 Tab 切换替代候选 → 自动学习

   长句预测：
   ─────────
     停顿 >300ms → 浮现淡灰续写 → 按 Tab 接受

   后台学习：
   ─────────
     无显式 query。最近 24h 输入即上下文。

   应急通道：
   ─────────
     长按 Ctrl → 唤起传统候选窗（99% 用不到）
```

---

## 二、范围铁律

### ✓ v1 做这些

- 拼音输入（全拼优先；双拼留 v0.2）
- 端侧个性化 rerank（~20M GRU）
- 上下文锁定（最近 24h 输入）
- Tab 切换替代候选
- 淡灰下一句预测（Qwen3-0.6B IQ4_XS 端侧）
- 本地 SQLite 数据明文，可视/可导/可删（捕获缓冲）
- **本地记忆完整能力（离线即用，明文 SQLite，可视/可导/可删）—— 核心机制**
- **可选：登入 ektro 后异步同步到 ektroai.com 喂养你的数字分身**
- **分身演化为能替你说话的 AI Agent（登入后）**
- 双进程崩溃隔离（IME 前端 ⇄ Core 后端）

### ✗ v1 不做（无例外，PR 直接拒）

- 候选窗默认显示
- 桌宠 / Live2D
- 打字时弹 Chat 浮窗（分身活在 ektroai.com，不在打字界面打扰）
- 语音输入
- 表情 / 颜文字 / 符号面板
- 皮肤商店
- 设置面板的 60 个开关（限制 ≤6 个）

> **想加新功能？先回答：它与"三戒"冲突吗？**
> 任何"yes"都意味着不做。

---

## 三、技术决策（无投票，已定）

| # | 决策 | 理由 |
|---|------|------|
| 1 | **Fork weasel + librime** | 站在十年成熟引擎肩上，不重造 TSF 轮子 |
| 2 | **词库用万象拼音** | amzxyz 已用 32GB 语料训出 8-gram |
| 3 | **Rerank 用 GRU ~20M** | 实时通路必须 ≤30ms，LLM 太重 |
| 4 | **预测用 Qwen3-0.6B IQ4_XS** | llama.cpp 端侧，停顿 300ms 触发 |
| 5 | **候选窗代码保留但默认隐藏** | 长按 Ctrl 应急通道 |
| 6 | **双进程模型** | Core 崩溃 → IME 自动降级到纯 librime |
| 7 | **本地明文 SQLite（捕获缓冲）** | 用户始终保有自己数据的明文副本，可导/可删 |
| 8 | **Apache 2.0 许可** | 开源就绪友好 |
| 9 | **云端分身同步走异步旁路** | 实时打字 100% 本地保 ≤30ms；同步不阻塞输入、离线补传 |
| 10 | **数据上行如实告知** | 不假装本地优先；README 隐私段明说"输入会离开本机" |

每条决策的展开理由见 `openspec/changes/ektro-mvp/design.md`（待建）。

---

## 四、性能 SLO（验收线）

任何破坏以下指标的 PR 都拒。

```
   首屏候选 (inline 字)        ≤ 50ms   P99
   GRU rerank 响应             ≤ 30ms   P99
   Qwen3 首 token 延迟         ≤ 200ms  P95
   内存常驻 (含 0.6B 量化)     ≤ 800MB
   CPU 空闲占用                < 1%
   每次 commit 落库异步        不阻塞输入
   实时打字通路                100% 本地，网络抖动零影响
   分身同步                    异步旁路，永不阻塞输入；离线补传
```

---

## 五、6 周路线（Cycle 1，Shape Up 风格）

```
Week 1   ▸ Fork weasel，编译，在 Windows 11 跑通
         ▸ 集成万象拼音词库
         ▸ 实测 Qwen3-0.6B IQ4_XS 在目标硬件首 token 延迟
         ▸ 决策日志：见 docs/decisions.md

Week 2   ▸ SQLite schema (commit_log, word_freq, phrase_pair)
         ▸ 每次 commit 异步落库
         ▸ 用户面板：能看到最近输入、能清空

Week 3   ▸ inline 渲染：候选 1 直接出现在光标位置
         ▸ 候选窗默认隐藏，长按 Ctrl 唤起
         ▸ Tab 切换替代候选

Week 4   ▸ GRU rerank 训练（用前两周积累的输入）
         ▸ 作为新 filter 加进 librime 管线
         ▸ 性能基准：rerank ≤30ms

Week 5   ▸ Qwen3-0.6B 集成（下一句预测）
         ▸ 淡灰渲染、Tab 接受、300ms 停顿触发
         ▸ 性能基准：首 token ≤200ms

Week 6   ▸ 5 天纯使用，记每个 bug 与"惊艳时刻"
         ▸ 修关键 bug
         ▸ 打 zip 自用
         ▸ 决定 Cycle 2 议题
```

### 退出标准（残酷诚实）

> ✓ 我能每天用它打字一整天，**没切回搜狗**。
> ✓ 它能学会我自定义的口头禅。
> ✓ 没有崩溃 / 卡死 / 数据丢失。

**6 周不达标 → 砍范围而不是延期。这是 Shape Up 铁律。**

---

## 六、目录结构（草拟，Week 1 确认）

```
E:\CLAUDE\EKTRO输入法\
├── CLAUDE.md                  ← 本文件（项目宪法）
├── README.md                  ← 公开介绍（开源时启用）
├── LICENSE                    ← Apache 2.0
│
├── openspec/                  ← OpenSpec 流程
│   └── changes/
│       └── ektro-mvp/
│           ├── proposal.md
│           ├── design.md
│           └── tasks.md
│
├── src/                       ← 源码
│   ├── ime-frontend/         ← TSF 前端 (fork from weasel)
│   ├── core-service/         ← 后端服务（Rust 候选）
│   ├── librime/              ← 引擎核心 (git submodule)
│   ├── ml/                   ← GRU rerank + Qwen3 集成
│   └── ui/                   ← 候选窗 + 淡灰预测渲染
│
├── data/
│   ├── dict/                 ← 万象拼音词库
│   └── models/               ← GGUF 模型文件
│
├── docs/
│   ├── decisions.md          ← 决策日志（含砍掉的）
│   ├── threat-model.md       ← 威胁模型
│   └── benchmarks/           ← 性能数据
│
└── tests/
    ├── latency/              ← 延迟基准
    └── correctness/          ← 候选准确率
```

---

## 七、与 AI 协作规范

### 模式与边界

| 模式 | 触发 | 允许 | 不允许 |
|------|------|------|--------|
| **探索** | `/openspec-explore` | 思考、调研、写决策文档 | 写代码 |
| **提案** | `/openspec-proposal` | 写 proposal/design/tasks | 跳过审核就执行 |
| **执行** | `/openspec-apply` | 按 tasks.md 执行 | 偏离已批准范围 |
| **归档** | `/openspec-archive` | 完成后归档变更 | 删除历史 |

### 通用规则

1. **中文交流**（默认）
2. **用 MCP 检索最新资料**，不依赖训练数据中可能过时的信息
3. **打字延迟永远是 P0**：任何让 P99 候选 >50ms 的改动 → 必须 design.md 评估
4. **三戒优先于功能**：如果一个 issue 想加的东西违反三戒，关闭 issue
5. **数据如实告知先于便利**：本地副本必须明文 + 可导出；输入会上行到 ektroai.com 这件事必须在 README/产品里明说，绝不淡化或隐藏
6. **小步前进**：单个 PR ≤ 500 行变更（除非纯依赖更新）
7. **每个 Cycle 结尾派 Agent Swarm 验收一次**（≥4 个独立维度：代码质量 / 静默失败 / 测试 / 接口设计）。不通过则不进入下一 Cycle。来源：D-009。
8. **绝不静默吞错**：任何 `except` 必须接 `logger`。没 log 的 except 视为 bug。来源：D-009。

### 给未来 AI 实例的话

如果你是 6 个月后接手这个项目的 Claude/AI 实例，欢迎。

- 阅读顺序：本文件 → `openspec/changes/` 最新变更 → `docs/decisions.md`
- 不确定时：**回到三戒**。三戒是这个项目的不动点。
- 用户身份：项目主理人，email `getwinccc@gmail.com`，Windows 11 重度键盘用户。
- 用户偏好：先用 MCP 检索、先做计划、不做未授权的大改、不污染 `.env`。

---

## 八、当前状态

- **阶段**：v0 立项完成，Cycle 1 待启动
- **下一步**：Week 1 探针（fork weasel + Qwen3 延迟实测 + 万象集成）
- **风险**：50% 失败概率（端侧延迟、首字准确率、用户习惯）
- **判断**：值得做。十年没人做过"取消候选窗"这件事。

---

## 九、不变的事

如果你对这份文档的任何修改有片刻怀疑，回到这三条：

> ### 1. **不打断视线。**
> ### 2. **记忆属于你。**
> ### 3. **越用越是你。**

其余皆可辩论。

> 注：2026-05-19 方向重定调 —— 从"纯本地输入法"转为"输入法 + 云端 AI 数字分身（ektroai.com）"。
> 旧三戒的"不离开磁盘 / 不解释自己"已被主理人正式推翻并替换为上方版本。
> 保留的不动点只剩 ①：打字当下永远安静、无窗、不打扰。
>
> 注：2026-05-20 三戒之②修订 —— 由"喂养你的分身（数据强制上行）"改为"记忆属于你（本地优先 + 上传是选择）"。
> 原承诺把"分身是终点"写成不可妥协的硬绑定，与第三方独立开源产品定位冲突。
> 新②把本地记忆主权立为根本承诺，链接 ektro 变为可选升级路径。
> 相关规范见 `docs/local-memory-schema.md` / `docs/ektro-link-protocol.md` / `docs/ime-ingest-contract.md` / `docs/privacy-boundary.md`。

---

*Last updated: 2026-05-20 · v0.3（记忆属于你 · 上传是选择）*
